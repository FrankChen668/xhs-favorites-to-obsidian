# SPEC：小红书收藏 → Obsidian 导出流水线（精简版 v0.4）

> 状态：Draft，待评审 ｜ 日期：2026-09-24

## 1. 要做什么
把你在小红书的**个人收藏（图文 + 视频）**导出成 Obsidian 可用的 Markdown，写进你指定的文件夹；导出后由 **WorkBuddy 窗口 AI** 帮你分组/打标签。纯 Python，**不装浏览器**。

## 2. 硬约束（红线）
- 不装 Chromium / Playwright，不模拟浏览器翻收藏。
- 只抓**自己的收藏**，不碰他人内容 / 评论 / 公开流。
- 视频**只存标题 + 链接**（不下载文件）。
- cookie **不硬编码、不提交**仓库。
- 抓取脚本**内不调 LLM**；分组靠 WorkBuddy 窗口 AI。

## 3. 核心流水线
1. 你从**现有 Chrome** 导出 `web_session` + `a1`（放 `secrets/`，gitignore）。
2. `curl_cffi`（Chrome 120 指纹）建会话 → 首页初始化拿 `webId`，**整会话复用**（防风控指纹抖动）。
3. `xhshow`（纯 Python）算 `x-s` / `x-t` 签名。
4. 翻 `collect/page`（cursor **顺序**）枚举全部收藏，按 `note_type` 分支：
   - 图文 → 详情 + 图片下载转 **webp** → MD
   - 视频 → 仅标题 / 链接 / 作者 / 时长 / 描述 → MD
5. 写入**你指定的文件夹**（Obsidian 直接打开，无需导入）。

## 4. 限速（务实档，可调）
- 单线程，随机 **2–5 秒** 间隔，约 **20 次/分**（远低于 100/分触发线，留余量）。
- 顺序翻页不跳页；遇 `412/461/300012/401` 指数退避，连败 3 次自动暂停。
- 参数在 `config.yaml` 可调；若遇滑块验证再调低。

## 5. 产出格式（简）
- frontmatter：`type` / `title` / `author` / `link` / `album?` / `tags` / `category` / `status: unstudied` / `summary?`
- 图文：正文 + `![[图片.webp]]`；视频：仅链接 + "未下载"提示。
- 图片体积：`image_max_width` 默认 720（省空间），可配。

## 6. 分组（导出后，WorkBuddy 窗口 AI 做，零配置）
- 我读 MD → 给**分类草案**（菜谱/穿搭/技术…）→ 你确认 → 回填 `category` / `tags` → 给 Dataview 索引。
- 若接口返回收藏夹字段，顺手按专辑建子目录（纯规则，无需 AI）。

## 7. 风控与合规（如实）
- 与"是否用浏览器"无关；读自己收藏 + 本人住宅 IP + 低频 = **低风险，非零**。
- 最坏后果限流/重登/滑块，**非永久封号**。
- 调用私有 API 违反 XHS ToS，仅限个人学习；不商用、不搬运公网。

## 8. 开工前需要你给的
- **输出文件夹绝对路径**：`F:\Obsidian\小红书`（已定，即 Obsidian vault 目录）
- **cookie**（`web_session` + `a1`）：从 Edge/Chrome DevTools 复制（两者同为 Chromium，操作一致）

给齐这两样，我就搭核心脚本并跑 **Phase 0 探针**（只读一页收藏验证连通性，**不下载、不动你的收藏**）。
